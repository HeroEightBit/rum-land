services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: rum-land:latest
    ports:
      - '4444:4444'
    environment:
      - NODE_ENV=production
    restart: unless-stopped
